import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
 images: {
    domains: ["localhost","picsum.photos",'api.gharbhetiba.mantracodex.com','127.0.0.1','gharbhetiba.mantracodex.com'],
  },
};

export default nextConfig;
